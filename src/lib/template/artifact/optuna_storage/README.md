# Artifact Optuna Storage

## Notes

- ★ Dockerized-NorLab project application (DNA) **required** directory.
- Directory `artifact/optuna_storage/` is required by `hydra-optuna-sweeper` dna configuration for hyperparam search.  
